{
  "name": "zinkex-blockchain-system",
  "version": "1.0.0",
  "scripts": {
    "test": "chmod +x node_modules/.bin/hardhat && npx hardhat test && jest --coverage",
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "@testing-library/react": "^14.0.0",
    "@walletconnect/web3-provider": "^1.8.0",
    "ethers": "^6.0.0",
    "hardhat": "^2.22.0",
    "jest": "^29.0.0",
    "node-fetch": "^3.3.2",
    "tailwindcss": "^3.4.0"
  }
}